# 📂 01.13 수업 내용  
## 📍 안내사항

## ✅ Java Generic Wildcard (제네릭 와일드 카드)

### 제네릭
- 클래스, 인터페이스, 메서드에 사용되는 자료형을 매개변수화 할 수 있는 
문법으로 다양한 타입에서 재사용이 가능

🛎️ 예시
```java
public class Box<T> {
    private T value;
    public void setValue(T value) {
        this.value = value;
    }
    public T getValue() {
        return value;
    }
}
```
이와 같은 방식으로 작성했을 때 value의 자료형을 고정시키지 않고 사용 가능함.  
예를들어 value에 String / Integer / Double / Product / Item ... etc 등으로 사용가능.  
즉 Java 자체의 자료형(class) 뿐만 아니라 개발자가 정의한 것도 집어넣을 수 있다.  
이는 추후 백엔드 설계에도 매우 중요함.

### 와일드카드
- 이상의 제네릭 타입을 사용할 때 구체적인 타입을 명시하지 않고 범용적으로 처리하고 싶을 때 사용함. `?` 사용
```java
public static void main(String[] args) {
    List<?> exampleList = new ArrayList<String>();
}
```
### 종류
1. 모든 타입 허용
    - 읽기 전용. 어떤 타입이든지 일단 받아놓고 분류해야 할때
2. T 또는 T의 서브 타입 (자식 클래스)
    - 읽기 전용. 제한된 타입에서 읽을 때
3. T 또는 T의 슈퍼 타입 (부모 클래스)
    - 쓰기 전용. 제한된 타입으로 넣을 때

## ✅ Collections
- 여러 객체들을 모아둔 것들을 의미함 -> 배열과는 약간 차이가 있음.
- 다만 Java에서의 Lst는 Python에서의 List와도 차이가 있음.

### 종류
1. List : 순서가 있는 데이터의 집합으로, 중복된 요소를 허락함.
    - ArrayList : 배열 기반의 리스트, element 접근 속도가 빠름.
    - LinkedList : 연결 리스트, 삽입 / 삭제 속도가 빠름.
```java
List<Person> list1 = new ArrayList<>();
list1.add("김일");    // 리스트 대입
System.out.println(list1);  // 리스트 출력 [김일]
list1.remove("김일");     // 리스트 삭제 (retrun 값은 boolean)
Collections.sort(list1);     // 리스트 정렬
```
2. Set : 중복을 허용하지 않는(List와의 차이 # 1) 테이터들의 집합
    - HashSet : 해시 테이블 기반의 집합. element의 순서를 보장하지 않음(List와의 차이 # 2)
    - TreeSet : 이진(binary) 검색 트리 기반의 집합. element가 정렬된 상태로 유지됨.
```java
Set<String> strSet = new HashSet<>();
strSet.add("java");     // 리스트와 동일
strSet.addAll(strList); // 리스트의 element들을 전부 Set에 대입
modifiedList.addAll(strSet);    // 다시 리스트로 형변환
Set<String> keySet = strMap1.keySet();  // keySet 
Collection<String> values = strMap1.values();   // valueSet
```
3. Map : Key / Value 쌍으로 이루어진 데이터들의 집합.
    - HashMap : 해시 테이블 기반의 맵. 키의 순서를 보장 x
    - TreeMap : 이진 검색 트리 기반의 맵. 키가 정렬된 상태로 유지.
```java
Map<String, String> strMap1 = new HashMap<>();
strMap1.put("kor2026001", "김일");    // 삽입 메서드 .put(키, 값)
strMap1.put("kor2026002", "김이");
strMap1.put("kor2026003", "김삼");
strMap1.put("kor2026004", "김사");
strMap1.put("kor2026005", "김오");
strMap1.put("kor2026005", "KimFive"); // 같은 key에 value를 입력할 경우
// 가장 최근 걸로 덮어쓰기 됨.
strMap1.replace("kor2026006", "KimSix");    // 'kor2026006' key가 존재하지 않기때문에 갱신 x
/*
   .put()의 경우에는 기존에 key가 존재한다면 value에 재대입하게 되겠지만
   없는 key라면 아예 key를 새로 생성.

   반면 .replace()의 경우에는 존재하지 않는 key라면 value의 갱신이 일어나지 않음.
   key가 존재할 때만 value의 값이 변경됨.
*/
strMap1.containsKey("kor2026001");  // 'kor2026001' key가 존재하는지 확인 : boolean형
strMap1.containsValue("김삼");    // '김삼' value가 존재하는지 확인
```
4. Entry : Key-Value 하나의 객체로 묶어서 사용
   - key와 value에 한번에 접근 가능.
   - .getKey(), .getValue()
```java
Set<Map.Entry<String, String>> entrySet1 = new HashSet<>();
```
### 참조 : JavaScript에서의 객체 정의 방식
```js
const person1 = {
    name : "김일",
    age : 20
};
```
-> Java에서의 Map과 유사한 형태를 가짐.  
-> 근데 JavaScript에서의 객체의 key-value 쌍을 property라고 부르지만  
Java에서의 Map의 key-value 쌍을 entry라고 부름.  
-> Java는 객체에서 field와 field의 value라고 볼 수 있고,  
JS에서는 property의 key-value라고 볼 수 있고,  
python에서는 attribute의 value라고 볼 수 있음.

# 🚨발생한 문제


# 📖 복습 & 확인
✔️ 제네릭 와일드카드, List, Set, Map, Entry  

💡📌📍🚩🚨⚠️